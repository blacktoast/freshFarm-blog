/** @jsx h */
import { h } from 'preact';
import { tw } from '@twind';
import CopyCode from '@/islands/CopyCode.tsx';

interface undefinedProps {}

const Home = ({}: undefinedProps) => {
  return <div>여기는 note</div>;
};

export default Home;
