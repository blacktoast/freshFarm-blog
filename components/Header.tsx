/** @jsx h */
import { h } from "preact";
import { tw } from "@twind";

interface HeaderProps {}

export const Header = ({}: HeaderProps) => {
  return (
    <div>
      <a href="/test">test</a>
    </div>
  );
};
